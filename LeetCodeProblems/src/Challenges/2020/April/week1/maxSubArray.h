#ifndef WEEK1_MAXSUBARRAY_H_
#define WEEK1_MAXSUBARRAY_H_
#include <bits/stdc++.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
using namespace std;

class maxSubArray {
public:
	maxSubArray();
	virtual ~maxSubArray();
	int maxsubArray(vector<int>&);
};

#endif /* WEEK1_MAXSUBARRAY_H_ */
